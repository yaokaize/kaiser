mybatisGenerator {
    verbose = true
    configFile = './src/main/resources/mybatis/generator.xml'
}

dependencies {
    compile project(':common')
    compile group: 'org.springframework.boot', name: 'spring-boot-starter-data-elasticsearch', version: "${springBootVersion}"
}